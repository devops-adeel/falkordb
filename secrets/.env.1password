# 1Password Secret Template for FalkorDB
# This file contains references to secrets stored in 1Password
# Vault: HomeLab (unified vault for all personal infrastructure)
#
# Usage:
#   op inject -i secrets/.env.1password -o /tmp/.env-$$
#
# Prerequisites:
# 1. Install 1Password CLI: brew install --cask 1password-cli
# 2. Sign in: op signin
# 3. Setup vault: make setup-vault

# =============================================================================
# Core Authentication
# =============================================================================

# Future Redis AUTH password (not currently enforced by FalkorDB)
# When enabled, this will secure Redis protocol access
FALKORDB_AUTH_PASSWORD=op://HomeLab/kjzbjtwils6teepbpgjpsqsp2a/redis-password

# NextAuth.js secret for session encryption
# Required for secure browser UI authentication
NEXTAUTH_SECRET=op://HomeLab/kjzbjtwils6teepbpgjpsqsp2a/nextauth-secret

# =============================================================================
# Integration APIs (Optional)
# =============================================================================

# Graphiti integration for knowledge graph operations
# Only needed if using Graphiti with FalkorDB
GRAPHITI_API_KEY=op://HomeLab/37e5lxhox53xsvzp3ozau32nha/graphiti-api-key

# OpenAI API key for LLM-powered features
# Only needed if using AI features with FalkorDB
OPENAI_API_KEY=op://HomeLab/37e5lxhox53xsvzp3ozau32nha/openai-api-key

# =============================================================================
# Future OAuth Providers (Optional)
# =============================================================================

# GitHub OAuth for SSO (future feature)
GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-not-configured}
GITHUB_CLIENT_SECRET=op://HomeLab/72353r457srqrjjjp4ocbqepta/github-client-secret

# Google OAuth for SSO (future feature)
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-not-configured}
GOOGLE_CLIENT_SECRET=op://HomeLab/72353r457srqrjjjp4ocbqepta/google-client-secret

# =============================================================================
# Notes
# =============================================================================
# - Backup configuration remains in .env (not secrets)
# - Performance settings remain in docker-compose.yml
# - This file is safe to commit (contains only references)
# - Actual secrets are stored securely in 1Password
# - Item IDs are used for references due to slashes in item names